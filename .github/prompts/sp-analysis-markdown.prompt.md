---
agent: 'agent'
name: sp-analysis-markdown
model: GPT-5.3-Codex (copilot)
description: Use the skill of 'sp-analysis' under `.github/skills/sp-analysis` to analyze the input and save the content in a markdown file. Return the file path directly and no need to show the analysis output to the user. The file name should be in the format of "Output_[object].md" under this path `AI_Agent_4_PLSQL/03Agent_Memory/`. The content should be saved in markdown format to preserve formatting and readability.
version: 1.1
---
When you are asked to save the output generated by you, follow these steps:
1. Abstract the key object in the user input as [object] and use the skill of 'sp-analysis' under `.github/skills/sp-analysis` to analyze it.
2. Create a markdown file with the name format "Output_[object].md" under this path `AI_Agent_4_PLSQL/03Agent_Memory/`.
  - If link to the package in `AI_Agent_4_PLSQL/02Development_Zone`,refer the path like `[JTA_Packages.sql:353](../02Development_Zone/Oracle_Package/JTA_Packages.sql#L353)`.
3. Save the generated content in the markdown file, preserving formatting and readability.
4. Return a clickable file path of the saved markdown file to the user.